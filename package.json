{
  "name": "expense-manager",
  "version": "1.4.0",
  "description": "Offline-first expense tracker (static PWA). Node is used for local development and tests; hosting stays static (Netlify).",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "test": "node tests/run-node-tests.js",
    "test:e2e": "playwright test tests/e2e.spec.js"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT",
  "devDependencies": {
    "@playwright/test": "^1.55.0"
  }
}
